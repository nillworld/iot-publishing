{
  "name": "websocket",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "concurrently \"cd ./back && npm build\" \"cd ./front && npm run build",
    "start": "concurrently \"cd ./back && npm start\" \"cd ./front && npm run start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.4.0"
  }
}
